#Part 1 Solution

#(2503 / fly + rest) * travellled  = distance

#To Do :
#Parse data (can calculate each reindeer as it is read it)
#Store data (dictionary matching reindeer to time)
#Compare values and select the heightest distance, return reindeer name
